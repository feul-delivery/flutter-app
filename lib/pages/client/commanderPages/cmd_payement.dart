import 'package:flutter/material.dart';

class CommandePayment extends StatelessWidget {
  const CommandePayment({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.red[900],
      ),
    );
  }
}
